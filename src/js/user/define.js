'use strict';
(function (window) {
    const BASE_PATH = "./";
    /**
     * 定義遊戲內部用到的常數即設定，例如是否為DEBUG模式
     * @type {
            {
                DEBUG: boolean,
            }
        }
     */
    window.define = {
        DEBUG: false,
    };

    // Object.freeze(window.define);
})(window);